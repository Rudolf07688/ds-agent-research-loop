[project]
name = "ds-agent-loop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rudolf Luttich", email = "rudolf@spatialedge.ai" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ds-agent-loop = "ds_agent_loop:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
